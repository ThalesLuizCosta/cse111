# def deposit (amount) :
    # Para que este programa funcione corretamente e para que os registros bancários estejam corretos, não devemos permitir que alguém deposite um valor zero ou negativo.
   # assert amount > 0

# Estes também são exemplos de de como se usar o "assert"
    # assert temperature < 0
    # assert len(given_name) > 0
    # assert balance == 0
    # assert school_year != "senior"

# def test_main():
   # assert min(7, -3, 0, 2) == -3

# As variáveis E e F podem ser quaisquer números de ponto flutuante de qualquer cálculo.
# e = 7.135
# f = 7.128

# if abs(e - f) < 0.01:
    # print(f"{e} and {f} are close enough so")
    # print("we'll consider them to be equal.")
# else:
    # print(f" {e} and {f} are not close and")
    # print("therefor not equal.")

